<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jwt.capability.mapper.SysUserRoleMapper">

    <resultMap type="SysUserRole" id="UserRoleResult">
        <result property="user_id" column="user_id"/>
        <result property="role_id" column="role_id"/>
    </resultMap>

    <delete id="deleteUserRoleByUserId" parameterType="String">
        delete
        from t_sys_user_role
        where user_id = #{user_id}
    </delete>

    <select id="countUserRoleByRoleId" resultType="Integer">
        select count(1)
        from t_sys_user_role
        where role_id = #{role_id}
    </select>

    <insert id="batchUserRole">
        insert into t_sys_user_role(user_id, role_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.user_id},#{item.role_id})
        </foreach>
    </insert>

</mapper>